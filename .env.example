# Optional. Set this to enable the machine-facing API at /api/v1/optimize
# (the endpoint n8n calls). Leave it unset and that route stays disabled and
# returns 503 — the web UI works either way and needs no configuration.
IMAGE_SEO_API_KEY=
